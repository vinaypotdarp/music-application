{
  "name": "ai-mode-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Root package.json for Vercel deployment — its only job is to give the /api serverless function (api/[...slug].js -> server/src/index.js) its dependencies during build. Local development still uses server/package.json and web/package.json directly (see SETUP.md / start-dev.bat).",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}
